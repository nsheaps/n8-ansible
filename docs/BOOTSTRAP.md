# Bootstrap Guide

This guide explains how to bootstrap a new host to join the ansible-managed infrastructure.

## Overview

The bootstrap process sets up a new host with:
- Required packages (ansible, git, 1Password CLI, etc.)
- SSH configuration and keys
- Ansible user for automation
- Scheduled ansible-pull for continuous configuration
- Host-specific configuration file

## Prerequisites

### 1Password Setup

Before bootstrapping, ensure you have the following items in 1Password:

1. **Ansible SSH Known Hosts** - A secure note containing SSH known_hosts entries
2. **Ansible User SSH Key** - An SSH key item with:
   - Private key field
   - Public key field

### GitHub Access

You'll need:
- A GitHub account with access to the ansible repository
- GitHub CLI authentication or a personal access token

## Quick Start

Run this single command to bootstrap a new host:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/nsheaps/n8-ansible/main/bin/bootstrap)
```

## Detailed Process

### 1. Platform Detection

The bootstrap script automatically detects:
- Operating system (macOS, Linux, WSL)
- Linux distribution (Ubuntu, Debian, Fedora, Arch, etc.)
- Desktop environment (for workstation vs server classification)

### 2. Package Installation

Based on your platform, the script installs:
- **Core tools**: git, curl, ca-certificates
- **Ansible**: For configuration management
- **1Password CLI**: For secret management
- **GitHub CLI**: For repository access
- **Gum**: For interactive prompts
- **SSH**: OpenSSH server/client

### 3. Service Configuration

The script configures:
- **SSH** (Optional): Only if you need remote access - asks before setup
- **1Password**: Authenticates with your 1Password account
- **GitHub**: Authenticates with GitHub for repository access

### 4. Repository Setup

The script:
- Clones the ansible repository to `~/src/n8-ansible`
- Generates a host_vars file for your hostname
- Adds your host to the inventory

### 5. Bootstrap Playbook

Runs the bootstrap playbook which:
- Retrieves SSH keys from 1Password
- Sets up the ansible user (if not exists)
- Collects SSH host keys for central storage
- Prepares the system for ansible-pull

### 6. First Ansible Pull

Optionally runs the first ansible-pull to:
- Apply all role configurations
- Set up scheduled pulls (every 30 minutes)
- Configure users, packages, and services

## Manual Steps

### After Bootstrap

1. **Review the generated host_vars file**:
   ```bash
   cat ~/src/n8-ansible/host_vars/$(hostname -s).yml
   ```

2. **Commit your changes**:
   ```bash
   cd ~/src/n8-ansible
   git add host_vars/$(hostname -s).yml
   git commit -m "Add host configuration for $(hostname -s)"
   git push
   ```

3. **Store collected SSH host keys** (if needed):
   - Review `host_keys_$(hostname -s).txt`
   - Add to your central SSH known_hosts store

## Platform-Specific Notes

### macOS

- SSH may need to be enabled in System Preferences > Sharing > Remote Login
- Homebrew will be installed if not present
- Uses Homebrew for package management

### WSL (Windows Subsystem for Linux)

- Detected automatically via `/proc/version`
- Treated as a workstation environment
- Uses standard Linux package managers

### Linux Distributions

#### Ubuntu/Debian
- Uses apt package manager
- Installs from official repositories
- 1Password CLI from official apt source

#### Fedora/RHEL/CentOS
- Uses dnf package manager
- Installs from official repositories
- 1Password CLI from official rpm repository

#### Arch Linux
- Uses pacman and AUR (via yay)
- Community packages from AUR

## Troubleshooting

### 1Password Authentication Issues

If you see "Not authenticated with 1Password", run:
```bash
eval $(op signin)
```

### SSH Connection Issues

Ensure SSH is enabled:
- **macOS**: System Preferences > Sharing > Remote Login
- **Linux**: `sudo systemctl enable --now ssh`

### Repository Access Issues

If you can't access the repository:
1. Check GitHub authentication: `gh auth status`
2. Re-authenticate if needed: `gh auth login`

### Package Installation Failures

- Ensure you have sudo access
- Check your internet connection
- Verify package manager repositories are accessible

## Configuration Files

### Generated host_vars

Located at `host_vars/$(hostname -s).yml`:
```yaml
---
# Configuration for hostname
# Generated by bootstrap script

# Ansible-pull configuration
ansible_pull_enabled: true
ansible_pull_cron_minute: "*/30"
ansible_pull_cron_hour: "*"

# User configuration
user_configs:
  - system_user: root
    config_profile: root
  - system_user: youruser
    config_profile: nsheaps
```

### Inventory Updates

Your host is added to `/hosts` under the appropriate group:
- `[workstation]` - For desktop systems
- `[server]` - For headless systems

## Security Considerations

- SSH keys are stored in 1Password and retrieved securely
- The ansible user has passwordless sudo (managed by ansible-pull role)
- All secrets are handled via 1Password CLI
- Temporary files are cleaned up after use

## Next Steps

After successful bootstrap:

1. Your system will automatically pull configuration every 30 minutes
2. You can manually trigger pulls: `sudo ansible-pull -U https://github.com/nsheaps/n8-ansible.git`
3. Customize your user profile in the host_vars file
4. Add any host-specific configurations

## Support

For issues or questions:
- Check the [main README](../README.md)
- Review ansible logs: `/var/log/ansible-pull.log`
- Open an issue on GitHub