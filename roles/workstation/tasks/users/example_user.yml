- name: users | example_user | remove preinstalled clutter from home directory
  file:
    path: /home/example_user/{{ item }}
    state: absent
  with_items:
    - examples.desktop
    - Desktop
    - Documents
    - Downloads
    - Music
    - Pictures
    - Public
    - Templates
    - Videos

- name: users | example_user | create personal config directories
  tags: dotfiles,dotfiles-example_user
  file:
    path: /home/example_user/{{ item.dir }}
    state: directory
    owner: example_user
    group: example_user
    mode: 0700
  with_items:
    - { dir: .config }
    - { dir: .config/autostart }
    - { dir: .config/bpytop }
    - { dir: .config/gtk-3.0 }
    - { dir: .config/terminator }
    - { dir: .local }
    - { dir: .local/share }
    - { dir: .local/share/applications }
    - { dir: bin }
    - { dir: desktop }
    - { dir: documents }
    - { dir: downloads }
    - { dir: projects }
    - { dir: templates }

- name: users | example_user | copy user-dirs.dirs
  tags: dotfiles,dotfiles-example_user
  copy:
    src: users/example_user/user-dirs.dirs
    dest: /home/example_user/.config/user-dirs.dirs
    owner: example_user
    group: example_user
    mode: 0600
  notify: update_xdg

- name: users | example_user | copy personal config files
  tags: dotfiles,dotfiles-example_user
  copy:
    src: users/example_user/{{ item.src }}
    dest: /home/example_user/{{ item.dest }}
    owner: example_user
    group: example_user
    mode: 0600
  with_items:
    - { src: 'bpytop.conf', dest: '.config/bpytop/bpytop.conf' }
    - { src: 'directory_bookmarks', dest: '.config/gtk-3.0/bookmarks' }
    - { src: 'gvimrc', dest: '.gvimrc' }
    - { src: 'ssh_client_config', dest: '.ssh/config' }
    - { src: 'terminator_config', dest: '.config/terminator/config' }

- name: users | example_user | copy profile image
  tags: dotfiles,dotfiles-example_user
  copy:
    src: users/example_user/profile_image.png
    dest: /var/lib/AccountsService/icons/example_user.png
    owner: root
    group: root
    mode: 0644
