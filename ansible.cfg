[defaults]
inventory = hosts
# log_path = /var/log/ansible.log  # Commented out - requires root privileges for local runs
retry_files_enabled = False
force_color = True
filter_plugins = ./filter_plugins
